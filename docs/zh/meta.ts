import { defineMeta } from 'blume'

export default defineMeta({
  // Chinese sidebar ordering (mirrors English structure but with Chinese titles via page frontmatter or folder meta)
  pages: ['index', 'data-spec', 'web-usage', 'native', 'protocols/built-in', 'api', 'faq'],
})
